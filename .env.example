# Clave de MapTiler (estilo vectorial + geocoding del buscador).
# Consíguela gratis en https://cloud.maptiler.com/account/keys/ y
# restríngela a tu dominio en el panel de MapTiler.
# Copia este fichero a `.env` (gitignored) y pega tu clave:
VITE_MAPTILER_KEY=
